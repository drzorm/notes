# git

### 初始项目
```git
mkdir wxapp_cnode
cd wxapp_cnode
git init
echo "# wxapp_cnode" >> README.md
git add README.md
git commit -m "first commit"
git remote add origin https://git.coding.net/drzorm/wxapp_cnode.git
git push -u origin master
```

### 已有项目
```git
git remote add origin https://git.coding.net/drzorm/wxapp_cnode.git
git push -u origin master
```
